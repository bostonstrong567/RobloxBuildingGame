-- Full GUI converter code for the Save/Load system
-- This was generated by the user using a Roblox GUI-to-Lua converter
-- The GUI is already placed in StarterGui.Main in Roblox Studio
--
-- Structure:
--   Main (ScreenGui)
--     GameState (Frame) - top bar with Save/Load buttons
--       ButtonHolder > SaveHolder > Save (TextButton)
--       ButtonHolder > LoadHolder > Load (TextButton)
--       ToggleVisability > ToggleSaveLoad (TextButton) - "Open" to show SaveManager
--     SaveManager (Frame) - main save panel with slot list
--       NewSaveButton > TextButton - "New Save"
--       LineSeparator
--       Lister (ScrollingFrame, auto-canvas-size Y)
--         SaveLoadTemplet (Frame) - TEMPLATE for each save slot
--           SaveNameText (TextLabel) - "Slot #1"
--           SaveLoad (Frame)
--             SaveTextButtonHolder > SaveTextButton - "Save to slot"
--             LoadButtonHolder > LoadButton - "Load from slot"
--     SaveSlot (Frame, initially Visible=false) - popup for naming new save
--       SaveTextBoxHolder > SaveTextBox (TextBox) - editable slot name
--       SaveButtonHolder > Save (TextButton) - confirm save
--
-- Button names for hooking up:
--   GameState.ButtonHolder.SaveHolder.Save -> opens SaveSlot popup
--   GameState.ButtonHolder.LoadHolder.Load -> opens SaveManager in load mode
--   GameState.ToggleVisability.ToggleSaveLoad -> toggles SaveManager visible
--   SaveManager.NewSaveButton.TextButton -> creates new slot
--   SaveLoadTemplet.SaveLoad.SaveTextButtonHolder.SaveTextButton -> save to that slot
--   SaveLoadTemplet.SaveLoad.LoadButtonHolder.LoadButton -> load from that slot
--   SaveSlot.SaveButtonHolder.Save -> confirm save with name from SaveTextBox
